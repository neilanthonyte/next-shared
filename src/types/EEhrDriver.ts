export enum EEhrDriver {
  Connect = "Connect",
  // helix for legacy support checks
  Helix = "Helix",
  // Future support, not used by anything else other than next-scripts yet
  nextEhr = "NextEhr",
}
